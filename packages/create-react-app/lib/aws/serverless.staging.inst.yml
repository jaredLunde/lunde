domain: <:(value += props.DOMAIN.split('.').length > 2 ? `staging-${props.DOMAIN}` : `staging.${props.DOMAIN}`):>
s3Bucket: staging-<:S3_BUCKET:>
hostedZone: <:(value += props.DOMAIN.split('.').slice(-2).join('.')):>.
environment:
  STAGE: ${self:provider.stage}
tags:
  stage: ${self:provider.stage}